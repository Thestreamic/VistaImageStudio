/** A cell rect as a fraction (0..1) of the collage canvas — scales to any size. */
export type { LayoutCollageTemplate as CollageTemplate } from './collage/template-registry'
export { COLLAGE_TEMPLATES } from './collage/template-registry'
